services:
  asi-chain-wallet:
    image: public.ecr.aws/f6y9h6x4/asi-chain/wallet:latest
    container_name: asi-chain-wallet
    ports:
      - "3000:80"
    environment:
      # Network Configuration
      # Configure available networks via JSON string
      - NETWORKS=${NETWORKS:-"{\"MainNet\":{\"ValidatorURL\":\"\",\"ReadOnlyURL\":\"\",\"IndexerURL\":\"\"},\"TestNet\":{\"ValidatorURL\":\"\",\"ReadOnlyURL\":\"\",\"IndexerURL\":\"\"},\"DevNet\":{\"ValidatorURL\":\"http://localhost:40403\",\"ReadOnlyURL\":\"http://localhost:40453\",\"IndexerURL\":\"http://localhost:8080/v1/graphql\"}}"}
    
    #healthcheck:
    #  test: ["CMD", "wget", "-q", "--spider", "http://localhost/health"]
    #  interval: 30s
    #  timeout: 10s
    #  retries: 3
    #  start_period: 40s
    
    restart: unless-stopped
    
    # If you need to connect to RChain nodes in a Docker network:
    # networks:
    #   - rchain-network

# If connecting to RChain Docker network:
# networks:
#   rchain-network:
#     external: true
#     name: node_rchain-network